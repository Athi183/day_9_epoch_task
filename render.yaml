services:
  - type: web
    name: day-9-epoch-task
    env: python
    branch: main
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.headless true
    pythonVersion: 3.13
